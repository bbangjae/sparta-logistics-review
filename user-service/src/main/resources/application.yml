server:
  port: ${USER_SERVICE_PORT}

spring:
  application:
    name: user-service
  config:
    import: "configserver:${CONFIG_SERVER_URL}"
  profiles:
    active: local

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_DEFAULT_ZONE}